作法：
用靜態分析的方法打開檔案後，仔細讀完Aseembly 的邏輯跟做法可以知道執行檔的行為如下：
1. 打開/tmp/flag 的檔案，如果不能打開或打開有誤則會報錯。
2. 開始把/tmp/flag 的東西讀出來。
3. 分成七段將兩邊的flag 輪流比較，但是存在執行檔內的flag 會用二補數的方法存，且交換al, ah 位置的資料。
4. 重複執行就可以還原flag。

遇到的問題：
1. 因為有對flag 做一些操作，所以還原的時候可能要搞清楚二補數跟交換al, ah 的順序。
2. 要清楚rax, eax, ax, al, ah 這些的記憶體位置儲存的位置。